１　Laravelプロジェクト作成 composer create-project laravel/laravel project-name
２ docker-compose.ymlファイル と dockerフォルダ をコピーして配置
３　docker compose build --no-cache && docker compose up -d
　　docker compose exec l12dev-app bash
　　composer install
    npm insatall
    npm install bootstrap --save
    npm install typescript --save-dev
    npx tsc --init
4 resources/ts フォルダを作成。resources/js配下のファイルを.tsに書き換えて配置する。
